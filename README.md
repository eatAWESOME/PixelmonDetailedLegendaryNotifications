# PixelmonDetailedLegendaryNotifications

When a legendary spawns, it sends the closest player a message containing the coordinates and it sends all other players a message containing who the closest player is.